# pybullet-based physics example code for SUNCG

Install dependencies with `pip3 install -r requirements`.

Run demo with `python3 demo.py` or directly `./demo.py`.

Change house id and parameters in `demo.py`.
